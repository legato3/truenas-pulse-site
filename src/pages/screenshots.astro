---
import BaseLayout from "~/components/BaseLayout.astro";
import Section from "~/components/Section.astro";
import ScreenshotSpotlight from "~/components/ScreenshotSpotlight.astro";
import Lightbox from "~/components/Lightbox.astro";
import CallToAction from "~/components/CallToAction.astro";
import { screenshots } from "~/data/screenshots";
---

<BaseLayout
  title="TrueNAS iPhone App Screenshots"
  description="See TrueNAS Pulse screenshots for iPhone: TrueNAS SCALE dashboard, storage health, sharing, workloads, fleet view, reporting, and Home Screen widget."
>
  <Section
    eyebrow="The app"
    heading="A walkthrough."
    headingLevel="h1"
    lede="Captured on iPhone 17. Tap any screen to view it full size."
  >
    <div class="spotlights">
      {
        screenshots.map((shot, i) => (
          <ScreenshotSpotlight shot={shot} index={i} eager={i === 0} />
        ))
      }
    </div>
  </Section>

  <Lightbox />
  <CallToAction />
</BaseLayout>

<style>
  .spotlights > * + * {
    border-top: 1px solid var(--color-stroke-soft);
  }
</style>
